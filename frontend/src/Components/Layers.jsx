import React from "react";

const Layers = () => {
  return (
    <aside className="column layers-column">
      <span className="column-title">LAYERS</span>
    </aside>
  );
};

export default Layers;
